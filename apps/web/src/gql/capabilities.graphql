query GetUserEffectiveCapabilities($clinicId: bigint!, $userId: uuid!) {
  clinic_user_effective_capabilities_v(where: { clinic_id: { _eq: $clinicId }, user_id: { _eq: $userId } }) {
    capability_key
    clinic_id
    user_id
  }
}
